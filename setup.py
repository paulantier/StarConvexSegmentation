from setuptools import setup, find_packages

setup(
    name='StarConvexSegmentation',
    version='0.1.0',
    description='A segmentation library, heavily inspired by StarDist',
    author='Paul Antier',
    packages=find_packages(),
    install_requires=[
        # Add dependencies here
    ],
)
